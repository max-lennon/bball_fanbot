services:
  db:
    image: postgres:16
    container_name: bball_postgres
    environment:
      POSTGRES_USER: max
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bball_db
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: